services:
  weather:
    build: .
    ports:
      - "4902:5000"
    volumes:
      - ./instance:/app/instance   # persist SQLite DB
      - ./importer.py:/app/importer.py  # optional, for running importer
    environment:
      - FLASK_ENV=production
    restart: unless-stopped
